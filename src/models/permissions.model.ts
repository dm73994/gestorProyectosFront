export interface permissionsModel {
    editUser: boolean;
    editRole: boolean;
    
    deleteUser: boolean;
    deleteRole: boolean;

    createUser: boolean;
    createRole: boolean;

    viewUser: boolean;
    viewRole: boolean;

    viewUsers: boolean;
    viewRoles: boolean;

    viewPermissions: boolean;

    activeUser: boolean;
}